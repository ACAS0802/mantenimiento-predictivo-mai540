# Tarea 2.2 — MAI 540. Versiones con las que se ejecutó y verificó el proyecto.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
# Solo para ejecutar el cuaderno en local (en Colab ya vienen incluidos):
notebook>=7.0
